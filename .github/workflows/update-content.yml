name: Update Content
on:
  push:
    branches:
      - master
  schedule:
    - cron: '0 0,12 * * *'

jobs:
  steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: '10.x'
    - run: yarn install
